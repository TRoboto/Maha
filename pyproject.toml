[tool.poetry]
name = "mahapy"
version = "0.1.0"
description = "Arabic Processing Library"
authors = ["TRoboto <m4bh@hotmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
